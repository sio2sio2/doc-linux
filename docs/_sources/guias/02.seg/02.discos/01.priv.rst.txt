Privacidad
**********
El primer aspecto que analizaremos relacionado con la seguridad en el
almacenamiento es el de privacidad de los datos, esto es, el impedir que un
usuario no autorizado pueda leerlos.

.. todo:: Este epígrafe de "Acceso al sistema" debería trasladarse a la unidad 1.

Acceso al sistema
=================
Trataremos bajo epígrafe estrictameente el acceso lógico al sistema a través de
algún mecanismo de :ref:`autenticación <pam>`. Centraremos nuestra
atención en varios aspectos:

Mecanismos de control de acceso
-------------------------------
.. seealso:: Se desarrolla en :ref:`el epígrafe homónimo del manual
   <control-accesos>`.`

Política de contraseñas
-----------------------
.. seealso:: Se desarrolla :ref:`dentro de la explicación sobre autenticación
   <politica-password>`.

Acceso a datos
==============
El epígrafe está centrado en el mecanismo de control de accesos |DAC|, pero dado
que el sistema |POSIX| de permisos (esto es, el sistema |UGO|), ya se estudió
en el módulo de :ref:`Sistemas Operativos Monopuesto <som>`, aquí se analizaran
las listas de control de accesos (|ACL|).

|ACL|
-----
.. seealso:: El contenido se encuentra dentro del manual en el :ref:`epígrafe
   dedidcado a permisos ACL <acls>`.

.. include:: /guias/02.seg/99.ejercicios/023.acl.rst

Cifrado de datos
================
Desde un punto de vista práctico y funcional, el cifrado de datos exige que las
operaciones de cifrado y descifrado se hagan de manera transparente, esto es,
sin que el usuario tenga que utilizar las herramientas criptográficas cada que
vez que desee guardar o leer un fichero. Hay tres estrategias para lograr este
cifrado transparente:

- Cifrar a bajo nivel, esto es, cifrar los bloques del disco duro y sobre estos
  bloques cifrados, construir el sistema de archivos.
- Cifrar utilizando el propio sistema de archivos, siempre que éste soporte tal
  posibilidad. Es el caso de sistemas modernos como |NTFS|, |ZFS| o *ext4*.
- Cifrar a alto nivel, esto es, cifrar ficheros y directorios antes de
  escribir sobre el sistema de ficheros

.. note:: El cifrado de discos se analizará detenidamente en el :ref:`epígrafe
   correspondiente de la unidad dedicada a la criptografía <disk-encrypt>`.

Eliminación de datos
====================

.. seealso:: Consulte :ref:`cómo eliminar datos en linux <remove-data>`.

.. |NTFS| replace:: :abbr:`NTFS (NT File System)`
.. |ZFS| replace:: :abbr:`ZFS (Zettabyte File System)`
.. |DAC| replace:: :abbr:`DAC (Discretionary Control Access)`
.. |UGO| replace:: :abbr:`UGO (User, Group, Other)`
.. |POSIX| replace:: :abbr:`POSIX (Portable Operating System Interface for uniX)`
